# Copy to deploy.local (gitignored). Do not put this file in git.
# Use SSH aliases, not public IPs, when you can.

# SSH_TARGET=my-ssh-alias
# HUB_HOST=my-ssh-alias
# HUB_URL=http://127.0.0.1:8788
# NODE_NAME=sg
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# FLEET_TOKEN=
